# =============================================================================
# Family Planner – environment variables
# =============================================================================
# Copy this file to .env.local and adjust values as needed.
#   cp .env.example .env.local
# Variables in .env.local are loaded by Next.js and are not committed to git.
# =============================================================================

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# Path to the SQLite database file (relative to project root or absolute).
# Default: sqlite.db in the project root.
DATABASE_URL="sqlite.db"

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
# Port for the Next.js dev and production server (npm run dev / npm run start).
PORT=3021

# -----------------------------------------------------------------------------
# Optional – production / SEO
# -----------------------------------------------------------------------------
# Set your public app URL for correct Open Graph, sitemap, and JSON-LD links.
# Only needed when deploying; leave commented for local development.
# Example: https://familyplanner.example.com
# NEXT_PUBLIC_APP_URL="https://your-domain.com"
