# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
#
# SENSITIVE / DO NOT COMMIT:
#   - .env* (except .env.example) → secrets, DB path, API URLs
#   - sqlite.db and *.db-* → database and SQLite WAL/journal files
#   - *.pem → keys/certificates

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (do not commit secrets)
.env*
!.env.example

# local SQLite database and WAL/journal files (do not commit)
sqlite.db
*.db-journal
*.db-wal
*.db-shm

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# personal plan seed (optional, keep local)
src/db/seed-plan.ts
